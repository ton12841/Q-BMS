# Core / session

Status: FOUNDATION ACTIVE

Session persistence is implemented by `auth_sessions` with HttpOnly browser tokens and hashed token storage. Real USER sessions will be created by the Google Workspace SSO flow.
