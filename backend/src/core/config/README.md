# Core / Configuration

Status: FOUNDATION ACTIVE

Canonical implementation lives under `backend/src/core/system-configuration/`.

System Configuration is a non-secret platform registry. Runtime secrets and environment credentials remain outside the database registry.
